import React from 'react';
import Logo from '../../atoms/Logo/Logo';
import './header.scss';

function Header() {
  return (
    <header className="header">
      <Logo />
    </header>
  );
}

export default Header;
